#!/usr/bin/python3
def update_dictionary(a_dictionary, key, value):
    a_dictionary[key] = value  # this line checks if a key exists AND a value otherwise it will create/replace

    return a_dictionary
